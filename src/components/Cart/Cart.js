import React from 'react';
import classes from './Cart.module.scss';

const cart = (props) => {
    return (
        <div className={classes.container}>
            <div className={classes.cart__wrap}></div>
        </div>
        
    );
}
 
export default cart;